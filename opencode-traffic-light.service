[Unit]
Description=opencode traffic light widget
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=@PYTHON@ @WIDGET@
Restart=no
Environment=DISPLAY=:0

[Install]
WantedBy=graphical-session.target
